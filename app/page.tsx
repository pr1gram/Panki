'use client'

import { useEffect, useMemo, useState } from 'react'
import { onAuthStateChanged, signInWithEmailAndPassword, signOut, createUserWithEmailAndPassword, type User } from 'firebase/auth'
import { doc, getDoc, setDoc, updateDoc, increment, serverTimestamp } from 'firebase/firestore'
import { auth, db, firebaseConfigured } from '@/lib/firebase'
import { BookOpen, CalendarDays, Check, ChevronRight, Flame, Headphones, Home, LayoutGrid, Moon, Play, Search, Settings, Sparkles, Sun, Trophy, Volume2 } from 'lucide-react'

type NavItem = { label: string; icon: typeof Home }
const nav: NavItem[] = [{ label: 'Home', icon: Home }, { label: 'Study', icon: BookOpen }, { label: 'Progress', icon: Trophy }, { label: 'Calendar', icon: CalendarDays }, { label: 'Browse', icon: LayoutGrid }]
const cards = [{ jp: 'おはよう', romaji: 'ohayou', en: 'good morning', type: 'Expression' }, { jp: 'ありがとう', romaji: 'arigatou', en: 'thank you', type: 'Expression' }, { jp: '学生', romaji: 'gakusei', en: 'student', type: 'Noun' }, { jp: '食べる', romaji: 'taberu', en: 'to eat', type: 'Verb' }]

function Logo() { return <div className="flex items-center gap-2"><div className="grid size-8 place-items-center rounded-xl bg-primary text-primary-foreground"><Sparkles size={16} /></div><span className="font-mono text-sm font-bold tracking-tight">lingua<span className="text-accent">.</span></span></div> }
function Stat({ label, value, detail, icon }: { label: string; value: string; detail: string; icon: React.ReactNode }) { return <div className="rounded-2xl border border-border bg-card p-4"><div className="flex items-center justify-between text-muted-foreground"><span className="text-xs">{label}</span>{icon}</div><p className="mt-3 font-mono text-2xl font-bold tracking-tight">{value}</p><p className="mt-1 text-xs text-muted-foreground">{detail}</p></div> }

function AuthGate({ onUser }: { onUser: (user: User) => void }) { const [mode, setMode] = useState<'signin' | 'signup'>('signin'); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const [loading, setLoading] = useState(false)
  const submit = async (e: React.FormEvent) => { e.preventDefault(); setLoading(true); setError(''); try { const result = mode === 'signin' ? await signInWithEmailAndPassword(auth!, email, password) : await createUserWithEmailAndPassword(auth!, email, password); onUser(result.user) } catch { setError('That email or password could not be used. Please try again.') } finally { setLoading(false) } }
  return <main className="grid min-h-screen place-items-center bg-background px-5 py-10"><div className="w-full max-w-md rounded-3xl border border-border bg-card p-7 shadow-sm md:p-9"><Logo /><p className="mt-12 font-mono text-[10px] uppercase tracking-widest text-accent">Your language practice space</p><h1 className="mt-3 text-3xl font-semibold tracking-tight">A little Japanese, every day.</h1><p className="mt-3 text-sm leading-6 text-muted-foreground">Build a calm, consistent habit with smart reviews that fit into your day.</p>{!firebaseConfigured && <p className="mt-5 rounded-xl bg-accent/10 p-3 text-xs text-accent">Firebase is not configured yet. Add the project variables to enable accounts.</p>}<form onSubmit={submit} className="mt-8 space-y-4"><label className="block text-sm font-medium">Email<input required type="email" value={email} onChange={e => setEmail(e.target.value)} className="mt-2 w-full rounded-xl border border-border bg-background px-3 py-3 text-sm outline-none focus:border-accent" placeholder="you@example.com" /></label><label className="block text-sm font-medium">Password<input required minLength={6} type="password" value={password} onChange={e => setPassword(e.target.value)} className="mt-2 w-full rounded-xl border border-border bg-background px-3 py-3 text-sm outline-none focus:border-accent" placeholder="At least 6 characters" /></label>{error && <p className="text-sm text-destructive">{error}</p>}<button disabled={loading || !firebaseConfigured} className="w-full rounded-xl bg-primary px-4 py-3 text-sm font-semibold text-primary-foreground disabled:opacity-50">{loading ? 'Please wait…' : mode === 'signin' ? 'Sign in' : 'Create account'}</button></form><button onClick={() => setMode(mode === 'signin' ? 'signup' : 'signin')} className="mt-5 w-full text-center text-sm text-muted-foreground hover:text-foreground">{mode === 'signin' ? 'New here? Create an account' : 'Already have an account? Sign in'}</button></div></main> }

function Sidebar({ active, setActive, dark, setDark, user }: { active: string; setActive: (v: string) => void; dark: boolean; setDark: (v: boolean) => void; user: User }) { return <aside className="hidden min-h-screen w-60 shrink-0 flex-col border-r border-border bg-card px-4 py-6 md:flex"><Logo /><div className="mt-10 flex flex-1 flex-col gap-1">{nav.map(({ label, icon: Icon }) => <button key={label} onClick={() => setActive(label)} className={`flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm transition ${active === label ? 'bg-primary text-primary-foreground shadow-sm' : 'text-muted-foreground hover:bg-muted hover:text-foreground'}`}><Icon size={17} />{label}</button>)}<div className="my-5 border-t border-border" /><button onClick={() => setActive('Achievements')} className={`flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm ${active === 'Achievements' ? 'bg-muted text-foreground' : 'text-muted-foreground hover:bg-muted'}`}><Trophy size={17} />Achievements</button><button onClick={() => setActive('Settings')} className={`flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm ${active === 'Settings' ? 'bg-muted text-foreground' : 'text-muted-foreground hover:bg-muted'}`}><Settings size={17} />Settings</button></div><div className="rounded-2xl border border-border bg-muted/50 p-3"><p className="font-mono text-[10px] uppercase tracking-widest text-muted-foreground">Current course</p><p className="mt-2 text-sm font-semibold">Japanese · JLPT N5</p><div className="mt-3 h-1.5 overflow-hidden rounded-full bg-border"><div className="h-full w-[22%] rounded-full bg-accent" /></div><p className="mt-2 text-xs text-muted-foreground">22% complete</p></div><button onClick={() => setDark(!dark)} className="mt-4 flex items-center gap-3 px-3 py-2 text-sm text-muted-foreground">{dark ? <Sun size={16} /> : <Moon size={16} />}{dark ? 'Light mode' : 'Dark mode'}</button><button onClick={() => signOut(auth!)} className="mt-1 px-3 py-2 text-left text-xs text-muted-foreground hover:text-foreground">Sign out · {user.email}</button></aside> }
function Topbar({ active, user }: { active: string; user: User }) { return <header className="flex items-center justify-between border-b border-border px-5 py-4 md:px-8"><div className="flex items-center gap-3 md:hidden"><Logo /></div><div className="hidden md:block"><p className="font-mono text-[10px] uppercase tracking-widest text-muted-foreground">Workspace / {active.toLowerCase()}</p><h1 className="mt-1 text-xl font-semibold tracking-tight">{active === 'Home' ? `Good morning, ${user.email?.split('@')[0] ?? 'learner'}.` : active}</h1></div><div className="flex items-center gap-3"><button className="hidden rounded-lg border border-border p-2 text-muted-foreground sm:block"><Search size={17} /></button><div className="grid size-8 place-items-center rounded-full bg-accent font-mono text-xs font-bold text-accent-foreground">{(user.email?.[0] ?? 'N').toUpperCase()}</div></div></header> }

function HomeView({ goStudy, stats }: { goStudy: () => void; stats: { streak: number; learned: number } }) { return <div className="space-y-8"><div className="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="text-sm text-muted-foreground">Tuesday, August 25, 2026</p><h2 className="mt-2 max-w-xl text-3xl font-semibold tracking-tight text-balance md:text-4xl">A little Japanese, every day.</h2><p className="mt-3 max-w-md text-sm leading-6 text-muted-foreground">Your daily lesson is ready. Keep your streak alive with just a few focused minutes.</p></div><button onClick={goStudy} className="flex items-center justify-center gap-2 rounded-xl bg-primary px-5 py-3 text-sm font-semibold text-primary-foreground shadow-sm transition hover:opacity-90"><Play size={16} fill="currentColor" /> Start today&apos;s lesson</button></div><div className="grid gap-4 sm:grid-cols-3"><Stat label="Day streak" value={String(stats.streak)} detail="Best: 18 days" icon={<Flame size={17} />} /><Stat label="Words learned" value={String(stats.learned)} detail="+8 this week" icon={<BookOpen size={17} />} /><Stat label="Study time" value="4h 32m" detail="This month" icon={<Headphones size={17} />} /></div><section className="rounded-2xl border border-border bg-card p-5 md:p-6"><div className="flex items-center justify-between"><div><p className="font-mono text-[10px] uppercase tracking-widest text-accent">Today&apos;s lesson</p><h3 className="mt-1 text-xl font-semibold">Everyday essentials</h3></div><span className="rounded-full bg-muted px-3 py-1 text-xs text-muted-foreground">8 min</span></div><div className="mt-6 flex items-center gap-4"><div className="relative size-20 shrink-0"><svg className="size-full -rotate-90" viewBox="0 0 36 36"><circle className="stroke-border" strokeWidth="3" fill="none" cx="18" cy="18" r="15" /><circle className="stroke-accent" strokeWidth="3" strokeLinecap="round" fill="none" cx="18" cy="18" r="15" strokeDasharray="94" strokeDashoffset="63" /></svg><span className="absolute inset-0 grid place-items-center font-mono text-xs">2/8</span></div><div><p className="text-sm font-medium">You&apos;re off to a good start.</p><p className="mt-1 text-sm leading-6 text-muted-foreground">Review familiar words, then meet a few new ones.</p></div></div></section></div> }

function StudyView({ finish, user }: { finish: () => void; user: User }) { const [index, setIndex] = useState(0); const [revealed, setRevealed] = useState(false); const card = cards[index % cards.length]; const answer = async () => { if (index >= 7) { await updateDoc(doc(db!, 'users', user.uid), { wordsLearned: increment(8), lastStudyAt: serverTimestamp() }).catch(() => undefined); finish() } else { setIndex(index + 1); setRevealed(false) } }; return <div className="mx-auto max-w-2xl space-y-6"><div className="flex items-center justify-between"><div><p className="font-mono text-[10px] uppercase tracking-widest text-accent">Daily lesson · Review</p><h2 className="mt-1 text-2xl font-semibold">Everyday essentials</h2></div><span className="font-mono text-sm text-muted-foreground">{index + 1} / 8</span></div><div className="h-1.5 overflow-hidden rounded-full bg-muted"><div className="h-full rounded-full bg-accent transition-all" style={{ width: `${((index + 1) / 8) * 100}%` }} /></div><div className="relative flex min-h-[360px] flex-col items-center justify-center rounded-3xl border border-border bg-card px-6 text-center shadow-sm"><span className="absolute left-5 top-5 rounded-full bg-muted px-3 py-1 font-mono text-[10px] uppercase tracking-widest text-muted-foreground">{card.type}</span><button aria-label="Play pronunciation" className="absolute right-5 top-5 rounded-full p-2 text-muted-foreground hover:bg-muted"><Volume2 size={18} /></button><p className="font-mono text-xs tracking-widest text-muted-foreground">Japanese</p><p className="mt-5 text-6xl font-medium tracking-tight">{card.jp}</p><p className="mt-3 text-base text-muted-foreground">{card.romaji}</p>{revealed ? <div className="mt-8 border-t border-border pt-5"><p className="text-xl font-semibold">{card.en}</p><p className="mt-1 text-sm text-muted-foreground">N5 · {card.type}</p></div> : <button onClick={() => setRevealed(true)} className="mt-10 rounded-xl bg-primary px-6 py-3 text-sm font-semibold text-primary-foreground">Show answer <span className="ml-2 font-mono text-xs opacity-60">Space</span></button>}</div>{revealed && <div className="grid grid-cols-4 gap-2">{['Again', 'Hard', 'Good', 'Easy'].map((label, i) => <button key={label} onClick={answer} className="rounded-xl border border-border bg-card p-3 text-center transition hover:border-accent"><span className="block text-xs font-semibold">{label}</span><span className="mt-1 block font-mono text-[10px] text-muted-foreground">{['1m', '6m', '10m', '4d'][i]}</span></button>)}</div>}</div> }

function GenericView({ active }: { active: string }) { const titles: Record<string, string> = { Progress: 'See your growth over time.', Calendar: 'Build a study rhythm.', Browse: 'Find your next word.', Achievements: 'Small wins add up.', Settings: 'Make lingua yours.' }; return <div className="space-y-8"><div><p className="text-sm text-muted-foreground">Your learning journey</p><h2 className="mt-2 text-3xl font-semibold tracking-tight">{titles[active] ?? 'Welcome back.'}</h2></div><div className="grid gap-4 md:grid-cols-3"><Stat label="Mastery" value="38%" detail="+6% this month" icon={<Trophy size={17} />} /><Stat label="Words reviewed" value="428" detail="Across 23 sessions" icon={<Check size={17} />} /><Stat label="Consistency" value="86%" detail="Better than 72% of learners" icon={<Flame size={17} />} /></div><section className="rounded-2xl border border-border bg-card p-6"><div className="flex items-center justify-between"><h3 className="text-lg font-semibold">{active === 'Calendar' ? 'August 2026' : active === 'Browse' ? 'Japanese N5 vocabulary' : active === 'Settings' ? 'Preferences' : 'Activity overview'}</h3><CalendarDays size={18} className="text-muted-foreground" /></div>{active === 'Browse' ? <div className="mt-6 grid gap-3 sm:grid-cols-2">{cards.map(card => <div key={card.jp} className="flex items-center justify-between rounded-xl border border-border p-4"><div><p className="text-lg font-medium">{card.jp}</p><p className="text-xs text-muted-foreground">{card.romaji} · {card.en}</p></div><ChevronRight size={16} className="text-muted-foreground" /></div>)}</div> : active === 'Settings' ? <div className="mt-6 space-y-4 text-sm"><div className="flex justify-between border-b border-border pb-4"><span>Course</span><span className="text-muted-foreground">Japanese · JLPT N5</span></div><div className="flex justify-between border-b border-border pb-4"><span>Daily goal</span><span className="text-muted-foreground">10 minutes</span></div><div className="flex justify-between"><span>Notifications</span><span className="text-accent">Enabled</span></div></div> : <div className="mt-8 grid grid-cols-7 gap-2">{Array.from({ length: 35 }, (_, i) => <div key={i} className={`aspect-square rounded-md border border-border ${[3, 4, 8, 9, 10, 15, 16, 21, 22, 27, 28, 33].includes(i) ? 'bg-accent' : 'bg-muted/40'}`} />)}</div>}<p className="mt-5 text-sm leading-6 text-muted-foreground">Keep showing up. Your consistency is the strongest predictor of long-term progress.</p></section></div> }

export default function Page() { const [user, setUser] = useState<User | null>(null); const [active, setActive] = useState('Home'); const [dark, setDark] = useState(false); const [stats, setStats] = useState({ streak: 12, learned: 142 }); useEffect(() => onAuthStateChanged(auth, async current => { setUser(current); if (current) { const snapshot = await getDoc(doc(db!, 'users', current.uid)); if (!snapshot.exists()) await setDoc(doc(db!, 'users', current.uid), { email: current.email, streak: 1, wordsLearned: 0, createdAt: serverTimestamp() }); else setStats({ streak: snapshot.data().streak ?? 12, learned: snapshot.data().wordsLearned ?? 142 }) } }), []); const content = useMemo(() => active === 'Home' ? <HomeView goStudy={() => setActive('Study')} stats={stats} /> : active === 'Study' && user ? <StudyView finish={() => setActive('Progress')} user={user} /> : <GenericView active={active} />, [active, stats, user]); if (!user) return <AuthGate onUser={setUser} />; return <div className={dark ? 'dark' : ''}><div className="flex min-h-screen bg-background text-foreground"><Sidebar active={active} setActive={setActive} dark={dark} setDark={setDark} user={user} /><main className="min-w-0 flex-1"><Topbar active={active} user={user} /><div className="mx-auto max-w-7xl px-5 py-8 pb-24 md:px-8 md:py-10">{content}</div></main><nav className="fixed inset-x-0 bottom-0 z-10 flex justify-around border-t border-border bg-card/95 px-2 py-2 backdrop-blur md:hidden">{nav.slice(0, 4).map(({ label, icon: Icon }) => <button key={label} onClick={() => setActive(label)} className={`flex flex-col items-center gap-1 px-3 py-1 text-[10px] ${active === label ? 'text-accent' : 'text-muted-foreground'}`}><Icon size={18} />{label}</button>)}</nav></div></div> }
